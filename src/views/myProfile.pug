extends base
include mixins/video

block content

    img(src="/" + user.avatarUrl, width="150")
    br
    h3=user.username
    hr
    nav 
          ul 
               each video in user.videos 
                    +video(video)
               else 
                    li sorry nothing found