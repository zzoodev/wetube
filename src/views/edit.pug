extends base
include mixins/video
block content   
    h3 Change Title of #{video.title}
     form(method="POST") 
        input(name="title", type="text", placeholders="Video Title", value=`${video.title}`,required)
        input(type="submit", value="Save")
                    
     